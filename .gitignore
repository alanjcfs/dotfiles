.DS_Store
.netrwhist
!tmp/swap/.gitkeep
!tmp/undo/.gitkeep
!tmp/backup/.gitkeep
tmp/
node_modules/

# Compiled Zsh scripts (bytecode cache)
*.zwc

# TPM-managed plugins (downloaded at runtime)
# Keep tpm itself (it's a submodule)
tmux/plugins/*
!tmux/plugins/tpm/

# ZSH plugins managed by powerlevel10k
zsh.d/Repos/

# vim-plug managed plugins (downloaded at runtime)
vim/plugged/
